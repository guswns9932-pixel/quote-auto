# CLAUDE.md

## 업데이트 안내 (WHATS_NEW) 자동 기입

`no_com/main.py`의 `WHATS_NEW` 리스트는 런처 실행 시 사용자에게 보여주는
업데이트 소식이다. **굵직한 기능 추가/변경을 커밋할 때마다, 사용자가 별도로
요청하지 않아도 이 리스트에 항목을 추가한다.**

- id는 `YYYY-MM-DD-slug` 형식 (날짜 접두로 정렬됨)
- title은 한 줄 요약, desc는 사용자가 체감할 효과 위주로 1~2문장
- **자잘한 버그 수정·내부 리팩터링·성능 미세조정은 올리지 않는다** —
  사용자가 "새로워졌다"고 느낄 만한 기능 추가/변경/큰 폭의 개선만 적는다
  (판단 기준: 이걸 사용자에게 말 안 하면 서운할 만한 변경인가?)
- 여러 개의 굵직한 변경을 한 번에 작업했다면 항목을 나누지 말고 하나로
  묶어도 된다 (예: "전자서명 속도·편의성 개선" 하나로).
- **app 필드 필수**: 업데이트 안내 창이 런처 버튼(앱)별 탭으로 나뉘어
  표시되므로, 항목마다 어느 앱 소식인지 `"app"` 키로 표시한다.
  값은 `"quote"`(견적서작성) / `"esign"`(전자서명) / `"search"`(키워드
  검색기) / `"csp"`(CSP UPLOAD 자동화) / `"launcher"`(특정 앱 하나가
  아니라 런처 자체 또는 여러 앱에 걸친 변경) 중 하나. 새 앱이 런처에
  추가되면 그 앱만의 새 app 값을 쓴다.
